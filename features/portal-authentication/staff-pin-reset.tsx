"use client";
import { useState,useTransition } from "react";
import { resetStaffPinAction } from "./actions";
export function StaffPinReset({members}:{members:readonly{id:string;name:string}[]}){const[pending,start]=useTransition();const[message,setMessage]=useState("");return <section className="rounded-2xl border bg-card p-5 sm:p-6"><h2 className="text-xl font-semibold">Acceso al Portal de Staff</h2><p className="mt-2 text-sm text-muted">Define o restablece el PIN personal de cuatro dígitos.</p><form className="mt-5 grid gap-3 md:grid-cols-[1fr_10rem_1fr_auto]" action={(data)=>start(async()=>{const result=await resetStaffPinAction(data);setMessage(result.ok?(result.message??"PIN actualizado."):(result.error??"No fue posible actualizar el PIN."))})}><select className="min-h-11 rounded-xl border bg-background px-3" name="staffId" required><option value="">Seleccionar Staff</option>{members.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><input className="min-h-11 rounded-xl border bg-background px-3" inputMode="numeric" maxLength={4} minLength={4} name="pin" pattern="[0-9]{4}" placeholder="PIN" required type="password"/><input className="min-h-11 rounded-xl border bg-background px-3" name="reason" placeholder="Razón del cambio" required/><button className="min-h-11 rounded-xl bg-brand px-4 font-semibold text-brand-foreground" disabled={pending}>{pending?"Guardando…":"Actualizar PIN"}</button></form>{message&&<p className="mt-3 text-sm text-muted">{message}</p>}</section>}
