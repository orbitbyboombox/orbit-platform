"use client";

import { useMemo, useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import { Banknote, FileText, HandCoins, Receipt, Search, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { StatusBadge } from "@/components/ui/status-badge";
import { attachHonorariosAction, markStaffMonthPaidAction, registerStaffAdvanceAction } from "./actions";

export type StaffPaymentEvent = { id:string; staffId:string; eventName:string; eventDate:string; amount:number; status:string };
export type StaffPaymentMonth = { id:string; staffId:string; month:string; tax:number; advances:number; paid:number; status:string; documents:{id:string;type:string;name:string;createdAt:string}[] };
export type StaffPaymentMember = { id:string; name:string; rut:string };
type PaymentAction = "ADVANCE" | "HONORARIOS" | "PAID";
type Panel = { type:PaymentAction; staff:StaffPaymentMember };
const money = new Intl.NumberFormat("es-CL", { style:"currency", currency:"CLP", maximumFractionDigits:0 });

export function StaffPaymentsCenter({ staff, events, months }:{ staff:StaffPaymentMember[]; events:StaffPaymentEvent[]; months:StaffPaymentMonth[] }) {
  const router=useRouter(); const[pending,startTransition]=useTransition(); const[month,setMonth]=useState(new Date().toISOString().slice(0,7)); const[query,setQuery]=useState(""); const[panel,setPanel]=useState<Panel|null>(null); const[error,setError]=useState("");
  const rows=useMemo(()=>staff.filter(member=>`${member.name} ${member.rut}`.toLowerCase().includes(query.toLowerCase())).map(member=>{const eventRows=events.filter(item=>item.staffId===member.id&&item.eventDate.startsWith(month));const record=months.find(item=>item.staffId===member.id&&item.month.startsWith(month));const honorarios=eventRows.reduce((sum,item)=>sum+item.amount,0);const tax=record?.tax??0;const net=Math.max(0,honorarios-tax);const advances=record?.advances??0;const paid=record?.paid??0;return{member,eventRows,record,honorarios,tax,net,advances,paid,outstanding:Math.max(0,net-advances-paid)}}),[staff,events,months,month,query]);
  const action=(form:FormData,type:PaymentAction)=>{setError("");startTransition(async()=>{const result=type==="ADVANCE"?await registerStaffAdvanceAction(form):type==="HONORARIOS"?await attachHonorariosAction(form):await markStaffMonthPaidAction(form);if(!result.ok){setError(result.error);return}setPanel(null);router.refresh()})};
  return <section aria-labelledby="staff-payments-title" className="space-y-5 rounded-2xl border bg-card p-5 sm:p-7">
    <header className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-brand">PAGOS DE STAFF</p><h2 className="mt-2 text-2xl font-semibold" id="staff-payments-title">Registro mensual</h2><p className="mt-2 text-sm text-muted">Pagos por evento, anticipos, honorarios y comprobantes en un solo registro.</p></div><div className="grid gap-2 sm:grid-cols-2"><label className="text-xs font-medium text-muted">Mes<input className="mt-1 h-11 w-full rounded-xl border bg-background px-3 text-sm text-foreground" onChange={event=>setMonth(event.target.value)} type="month" value={month}/></label><label className="text-xs font-medium text-muted">Buscar<div className="relative mt-1"><Search className="absolute left-3 top-3.5 size-4"/><input className="h-11 w-full rounded-xl border bg-background pl-9 pr-3 text-sm text-foreground" onChange={event=>setQuery(event.target.value)} placeholder="Nombre o RUT" value={query}/></div></label></div></header>
    {error&&<p className="rounded-xl border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-600">{error}</p>}
    <div className="grid gap-4 xl:grid-cols-2">{rows.map(row=><article className="rounded-2xl border p-4 sm:p-5" key={row.member.id}><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-semibold">{row.member.name}</p><p className="mt-1 text-sm text-muted">{row.member.rut} · {row.eventRows.length} eventos</p></div><StatusBadge label={row.record?.status==="PAID"?"Pagado":row.record?.status==="PARTIALLY_PAID"?"Pago parcial":"Pendiente"} variant={row.record?.status==="PAID"?"success":row.record?.status==="PARTIALLY_PAID"?"info":"warning"}/></div><dl className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-4"><Metric label="Honorarios" value={row.honorarios}/><Metric label="Impuesto" value={row.tax}/><Metric label="Pago neto" value={row.net}/><Metric label="Pendiente" value={row.outstanding}/></dl><div className="mt-4 grid gap-2 text-sm sm:grid-cols-2"><p className="rounded-lg bg-background/50 p-3"><span className="block text-xs text-muted">Anticipos</span>{money.format(row.advances)}</p><p className="rounded-lg bg-background/50 p-3"><span className="block text-xs text-muted">Saldo pagado</span>{money.format(row.paid)}</p></div><div className="mt-4 space-y-2">{row.eventRows.map(item=><div className="flex justify-between gap-3 border-b py-2 text-sm" key={item.id}><span>{item.eventName}</span><strong>{money.format(item.amount)}</strong></div>)}</div><div className="mt-4 flex flex-wrap gap-2 border-t pt-4"><Button className="min-h-9 px-3" onClick={()=>setPanel({type:"ADVANCE",staff:row.member})} variant="outline"><HandCoins className="size-4"/>Registrar anticipo</Button><Button className="min-h-9 px-3" onClick={()=>setPanel({type:"HONORARIOS",staff:row.member})} variant="outline"><FileText className="size-4"/>Adjuntar honorarios</Button><Button className="min-h-9 px-3" disabled={row.record?.status==="PAID"} onClick={()=>setPanel({type:"PAID",staff:row.member})}><Receipt className="size-4"/>Marcar pagado</Button></div>{row.record?.documents.length?<p className="mt-3 text-xs text-muted">{row.record.documents.length} documentos vinculados al mes.</p>:null}</article>)}{!rows.length&&<div className="rounded-xl border border-dashed p-8 text-center xl:col-span-2"><Banknote className="mx-auto size-7 text-muted"/><p className="mt-3 font-semibold">Sin Staff para mostrar</p><p className="mt-1 text-sm text-muted">Los pagos aparecerán cuando exista personal activo.</p></div>}</div>
    {panel&&<PaymentPanel error={error} month={month} onClose={()=>setPanel(null)} onSubmit={action} panel={panel} pending={pending}/>} 
  </section>
}

function PaymentPanel({panel,month,pending,error,onClose,onSubmit}:{panel:Panel;month:string;pending:boolean;error:string;onClose:()=>void;onSubmit:(data:FormData,type:PaymentAction)=>void}){return <div aria-modal="true" className="fixed inset-0 z-50 flex items-end justify-center bg-black/60 sm:items-center sm:p-6" role="dialog"><div className="w-full rounded-t-2xl border bg-card p-5 sm:max-w-xl sm:rounded-2xl sm:p-7"><div className="flex justify-between"><div><p className="text-xs font-semibold text-brand">{panel.staff.name}</p><h3 className="mt-1 text-xl font-semibold">{panel.type==="ADVANCE"?"Registrar anticipo":panel.type==="HONORARIOS"?"Adjuntar honorarios":"Marcar mes pagado"}</h3></div><button aria-label="Cerrar" className="rounded-lg border p-2" onClick={onClose}><X className="size-4"/></button></div><form action={data=>onSubmit(data,panel.type)} className="mt-6 grid gap-4"><input name="staffId" type="hidden" value={panel.staff.id}/><input name="month" type="hidden" value={month}/>{panel.type==="ADVANCE"&&<><Field label="Monto del anticipo" name="amount" required type="number"/><Field label="Motivo" name="notes"/><File accept="application/pdf,image/jpeg,image/png,image/webp" label="Comprobante del anticipo (opcional)" name="proof"/></>}{panel.type==="HONORARIOS"&&<File accept="application/pdf" label="PDF de honorarios" name="file" required/>}{panel.type==="PAID"&&<><Field label="Impuesto retenido" name="taxAmount" required type="number"/><File accept="application/pdf,image/jpeg,image/png,image/webp" label="Comprobante de pago" name="proof" required/></>}{error&&<p className="text-sm text-red-600">{error}</p>}<Button disabled={pending}>{pending?"Guardando...":"Guardar registro"}</Button></form></div></div>}
function Metric({label,value}:{label:string;value:number}){return <div className="rounded-lg border p-3"><dt className="text-xs text-muted">{label}</dt><dd className="mt-1 text-sm font-semibold">{money.format(value)}</dd></div>}
function Field({label,name,type="text",required}:{label:string;name:string;type?:string;required?:boolean}){return <label className="text-sm font-medium">{label}<input className="mt-2 h-11 w-full rounded-xl border bg-background px-3" min={type==="number"?0:undefined} name={name} required={required} type={type}/></label>}
function File({label,name,accept,required}:{label:string;name:string;accept:string;required?:boolean}){return <label className="text-sm font-medium">{label}<input accept={accept} className="mt-2 block w-full rounded-xl border bg-background p-2 text-sm" name={name} required={required} type="file"/></label>}
