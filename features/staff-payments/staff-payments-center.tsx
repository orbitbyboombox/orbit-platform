"use client";
import {useMemo,useState}from"react";
import{ExternalLink,Search,TriangleAlert}from"lucide-react";
import{StatusBadge}from"@/components/ui/status-badge";

export type StaffPaymentEvent={id:string;staffId:string;projectId:string;eventName:string;eventDate:string;accountingMonth:string;customer:string;service:string;durationHours:number;roles:string[];amount:number;originalNet:number;adjustmentTotal:number;reimbursementTotal:number;finalAmount:number;operator:number;assembly:number;disassembly:number;overrideReason:string;status:string;settlementStatus:string;paidAmount:number;paidAt:string;receiptStatus:string};
export type StaffPaymentMonth={id:string;staffId:string;month:string;tax:number;advances:number;paid:number;status:string;documents:{id:string;type:string;name:string;createdAt:string}[]};
export type StaffPaymentMember={id:string;name:string;rut:string};
const money=new Intl.NumberFormat("es-CL",{style:"currency",currency:"CLP",maximumFractionDigits:0});const roleLabel=(value:string)=>({OPERATOR:"Operador",ASSEMBLY:"Montaje",DISASSEMBLY:"Desmontaje"}[value]??value);

export function StaffPaymentsCenter({staff,events}:{staff:StaffPaymentMember[];events:StaffPaymentEvent[];months:StaffPaymentMonth[]}){const[month,setMonth]=useState(new Date().toISOString().slice(0,7));const[query,setQuery]=useState("");const rows=useMemo(()=>staff.filter(member=>`${member.name} ${member.rut}`.toLowerCase().includes(query.toLowerCase())).map(member=>{const eventRows=events.filter(item=>item.staffId===member.id&&item.accountingMonth.startsWith(month));const original=eventRows.reduce((sum,item)=>sum+item.originalNet,0),adjustments=eventRows.reduce((sum,item)=>sum+item.adjustmentTotal,0),reimbursements=eventRows.reduce((sum,item)=>sum+item.reimbursementTotal,0),payrollNet=original+adjustments,finalAmount=payrollNet+reimbursements,paid=eventRows.reduce((sum,item)=>sum+item.paidAmount,0),companyCost=Math.round(payrollNet/(1-.1525))+reimbursements;return{member,eventRows,original,adjustments,reimbursements,finalAmount,companyCost,paid,outstanding:eventRows.reduce((sum,item)=>sum+Math.max(0,item.finalAmount-item.paidAmount),0)}}),[events,month,query,staff]);return <section className="space-y-5 rounded-2xl border bg-card p-5 sm:p-7"><header className="flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-brand">Staff · Registro mensual</p><h2 className="mt-2 text-2xl font-semibold">Liquidaciones por mes contable</h2><p className="mt-2 text-sm text-muted">La nómina usa el mes contable del pago. La fecha del Evento se muestra solo como referencia histórica.</p></div><div className="grid gap-2 sm:grid-cols-2"><label className="text-xs text-muted">Mes<input className="mt-1 h-11 w-full rounded-xl border bg-background px-3" onChange={e=>setMonth(e.target.value)} type="month" value={month}/></label><label className="text-xs text-muted">Buscar<div className="relative mt-1"><Search className="absolute left-3 top-3.5 size-4"/><input className="h-11 w-full rounded-xl border bg-background pl-9 pr-3" onChange={e=>setQuery(e.target.value)} value={query}/></div></label></div></header><div className="grid gap-4 xl:grid-cols-2">{rows.map(row=><details className="rounded-2xl border p-4 sm:p-5" key={row.member.id}><summary className="cursor-pointer list-none"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold">{row.member.name}</p><p className="mt-1 text-sm text-muted">{new Date(`${month}-01T12:00:00Z`).toLocaleDateString("es-CL",{month:"long",year:"numeric"})} · {row.eventRows.length} eventos</p></div><StatusBadge label={row.outstanding===0&&row.finalAmount?"Pagado":row.paid?"Pago parcial":"Pendiente"} variant={row.outstanding===0&&row.finalAmount?"success":row.paid?"info":"warning"}/></div><dl className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-3"><Metric label="Neto original" value={row.original}/><Metric label="Ajustes" value={row.adjustments}/><Metric label="Reembolsos" value={row.reimbursements}/><Metric label="Monto final" value={row.finalAmount}/><Metric label="Ya pagado" value={row.paid}/><Metric label="Saldo pendiente" value={row.outstanding}/><Metric label="Costo empresa" value={row.companyCost}/><Metric label="Boletas pendientes" value={row.eventRows.filter(x=>x.receiptStatus!=="RECEIVED").length}/><Metric label="Boletas recibidas" value={row.eventRows.filter(x=>x.receiptStatus==="RECEIVED").length}/><Metric label="Eventos trabajados" value={row.eventRows.length}/></dl><p className="mt-3 text-xs font-semibold text-brand">Abrir detalle mensual</p></summary><div className="mt-4 space-y-3">{row.eventRows.map(item=><EventRow item={item} key={item.id}/>)}</div></details>)}</div></section>}
function EventRow({item}:{item:StaffPaymentEvent}){return <article className="rounded-xl border p-3 text-sm"><div className="flex justify-between gap-3"><div><strong>{item.eventName}</strong><p className="mt-1 text-xs text-muted">{item.eventDate} · {item.customer}</p><p className="mt-1 text-xs text-muted">{item.service} · {item.durationHours} horas · {item.roles.map(roleLabel).join(" + ")}</p></div><StatusBadge label={item.settlementStatus==="PAID"?"Pagado":item.settlementStatus==="ADVANCE"?"Anticipo":"Pendiente"} variant={item.settlementStatus==="PAID"?"success":item.settlementStatus==="ADVANCE"?"info":"warning"}/></div><div className="mt-3 grid grid-cols-2 gap-2 text-xs sm:grid-cols-3"><Box label="Original" value={money.format(item.originalNet)}/><Box label="Ajustes" value={money.format(item.adjustmentTotal)}/><Box label="Reembolsos" value={money.format(item.reimbursementTotal)}/><Box label="Monto final" value={money.format(item.finalAmount)}/><Box label="Anticipo / pagado" value={money.format(item.paidAmount)}/><Box label="Saldo" value={money.format(Math.max(0,item.finalAmount-item.paidAmount))}/></div><p className={`mt-3 flex items-center gap-2 text-xs font-semibold ${item.receiptStatus==="RECEIVED"?"text-emerald-500":"text-amber-500"}`}><TriangleAlert className="size-4"/>{item.receiptStatus==="RECEIVED"?"Boleta recibida":"Boleta pendiente"}</p><a className="mt-3 inline-flex items-center gap-1 text-xs font-semibold text-brand" href={`/projects/${item.projectId}#staff-assignment`}><ExternalLink className="size-3.5"/>Abrir liquidación en Evento</a></article>}
function Metric({label,value}:{label:string;value:number}){const financial=!label.startsWith("Boletas")&&label!=="Eventos trabajados";return <div className="rounded-lg border p-3"><dt className="text-xs text-muted">{label}</dt><dd className="mt-1 text-sm font-semibold">{financial?money.format(value):value}</dd></div>}
function Box({label,value}:{label:string;value:string}){return <span className="rounded-lg bg-background/50 p-2">{label}<br/><b>{value}</b></span>}
