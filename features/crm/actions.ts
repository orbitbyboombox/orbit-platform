"use server";
import{revalidatePath}from"next/cache";import{createSupabaseServerClient}from"@/lib/supabase/server";
const message=(error:unknown,fallback:string)=>error instanceof Error?error.message:fallback;
async function founderClient(){const client=await createSupabaseServerClient();const{data}=await client.auth.getUser();if(!data.user)throw new Error("Sesión requerida.");const{data:profile,error}=await client.from("profiles").select("role").eq("id",data.user.id).single();if(error)throw error;if(!["CEO","ADMINISTRATOR"].includes(profile.role))throw new Error("Solo Founder o Administración puede gestionar clientes.");return{client,user:data.user}}
export async function createCrmCustomerAction(input:{fullName:string;rut:string;company:string;phone:string;email:string;address:string}){try{const{client,user}=await founderClient();if(!input.fullName.trim()||!input.rut.trim())throw new Error("Nombre y RUT son obligatorios.");const normalized=input.rut.toUpperCase().replace(/[^0-9K]/g,"");const{data:existing,error:lookup}=await client.from("customers").select("id").is("deleted_at",null).eq("rut",input.rut.trim()).maybeSingle();if(lookup)throw lookup;if(existing)throw new Error("Ya existe un cliente con este RUT.");const{data,error}=await client.from("customers").insert({full_name:input.fullName.trim(),rut:input.rut.trim(),company:input.company.trim()||null,phone:input.phone.trim()||null,email:input.email.trim().toLowerCase()||null,address:input.address.trim()||null,metadata:{normalizedRut:normalized},created_by:user.id,updated_by:user.id}).select("id").single();if(error)throw error;revalidatePath("/customers");return{ok:true as const,id:data.id}}catch(error){return{ok:false as const,error:message(error,"No fue posible crear el cliente.")}}}
export async function updateCrmCustomerAction(input:{id:string;fullName:string;rut:string;company:string;phone:string;email:string;address:string;commercialNotes:string;reason:string}){try{const{client,user}=await founderClient();if(!input.reason.trim())throw new Error("Registra el motivo del cambio.");const{data:current,error:readError}=await client.from("customers").select("metadata").eq("id",input.id).single();if(readError)throw readError;const{error}=await client.from("customers").update({full_name:input.fullName.trim(),rut:input.rut.trim(),company:input.company.trim()||null,phone:input.phone.trim()||null,email:input.email.trim().toLowerCase()||null,address:input.address.trim()||null,metadata:{...current.metadata,commercialNotes:input.commercialNotes},approval_reason:input.reason,updated_by:user.id}).eq("id",input.id).is("deleted_at",null);if(error)throw error;revalidatePath(`/customers/${input.id}`);revalidatePath("/customers");return{ok:true as const}}catch(error){return{ok:false as const,error:message(error,"No fue posible actualizar el cliente.")}}}
export async function archiveCrmCustomerAction(id:string,reason:string){try{const{client,user}=await founderClient();if(reason.trim().length<3)throw new Error("El motivo de archivo es obligatorio.");const{count,error:eventsError}=await client.from("projects").select("id",{count:"exact",head:true}).eq("customer_id",id).is("deleted_at",null);if(eventsError)throw eventsError;if((count??0)>0)throw new Error("Archiva o cancela primero los eventos activos del cliente.");const{error}=await client.from("customers").update({deleted_at:new Date().toISOString(),deleted_by:user.id,approval_reason:reason,updated_by:user.id}).eq("id",id).is("deleted_at",null);if(error)throw error;revalidatePath("/customers");return{ok:true as const}}catch(error){return{ok:false as const,error:message(error,"No fue posible archivar el cliente.")}}}
export async function mergeCrmCustomersAction(masterCustomerId:string,duplicateCustomerId:string,reason:string){try{const{client}=await founderClient();const{data,error}=await client.rpc("merge_crm_customers",{p_master_customer_id:masterCustomerId,p_duplicate_customer_id:duplicateCustomerId,p_reason:reason});if(error)throw error;revalidatePath("/customers");revalidatePath(`/customers/${masterCustomerId}`);return{ok:true as const,result:data}}catch(error){return{ok:false as const,error:message(error,"No fue posible fusionar los clientes.")}}}
