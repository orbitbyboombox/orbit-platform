export type OperationalStartSource="STAFF_CALL"|"STAFF_ARRIVAL"|"SERVICE_START";
export type OperationalWindow={operationalStartAt:string;serviceStartAt:string;serviceEndAt:string;source:OperationalStartSource};
const valid=(value:string|null|undefined):value is string=>typeof value==="string"&&!Number.isNaN(new Date(value).getTime());
export function isValidOperationalCall(value:string|null|undefined,serviceStartAt:string){if(typeof value!=="string"||!valid(value)||!valid(serviceStartAt))return false;const call=new Date(value).getTime(),start=new Date(serviceStartAt).getTime();return call<=start&&call>=start-86_400_000;}
export const chileDateTime=(value:string)=>{const parts=new Intl.DateTimeFormat("en-CA",{timeZone:"America/Santiago",year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).formatToParts(new Date(value));const part=(type:Intl.DateTimeFormatPartTypes)=>parts.find(item=>item.type===type)?.value??"";return{date:`${part("year")}-${part("month")}-${part("day")}`,time:`${part("hour")}:${part("minute")}`}};
export function resolveEventOperationalWindow(input:{assignmentStaffCalls:readonly(string|null|undefined)[];staffArrivalAt?:string|null;serviceStartAt:string;serviceEndAt:string}):OperationalWindow{const earliestCall=input.assignmentStaffCalls.filter((value):value is string=>isValidOperationalCall(value,input.serviceStartAt)).sort((left,right)=>new Date(left).getTime()-new Date(right).getTime())[0];const arrival=isValidOperationalCall(input.staffArrivalAt,input.serviceStartAt)?input.staffArrivalAt:null;return{operationalStartAt:earliestCall??arrival??input.serviceStartAt,serviceStartAt:input.serviceStartAt,serviceEndAt:input.serviceEndAt,source:earliestCall?"STAFF_CALL":arrival?"STAFF_ARRIVAL":"SERVICE_START"};}
