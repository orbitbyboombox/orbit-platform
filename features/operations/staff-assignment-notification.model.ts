import {createHash} from "node:crypto";

export type StaffAssignmentMail={to:string;subject:string;textBody:string;htmlBody:string;driveFileIds:string[]};
export const assignmentRoleLabel=(value:string)=>({OPERATOR:"Operador",ASSEMBLY:"Montaje",DISASSEMBLY:"Desmontaje"}[value]??value);
const escape=(value:unknown)=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[char]??char));
export const chileAssignmentDateTime=(value:string|null)=>value?new Intl.DateTimeFormat("es-CL",{dateStyle:"short",timeStyle:"short",timeZone:"America/Santiago"}).format(new Date(value)):"Por confirmar";
export function assignmentNotificationFingerprint(input:{assignmentId:string;staffId:string;role:string;staffCallAt:string|null;serviceStartAt:string|null;serviceEndAt:string|null;location:string|null}){return createHash("sha256").update(JSON.stringify(input)).digest("hex").slice(0,24)}
export function buildStaffAssignmentEmail(input:{staffName:string;eventName:string;orbitEventId:string;role:string;staffCallAt:string|null;serviceStartAt:string|null;serviceEndAt:string|null;location:string|null;portalUrl:string}):StaffAssignmentMail{const call=chileAssignmentDateTime(input.staffCallAt),start=chileAssignmentDateTime(input.serviceStartAt),end=chileAssignmentDateTime(input.serviceEndAt),subject=`Nueva asignación BOOMBOX · ${input.orbitEventId}`;return{to:"",subject,textBody:`Hola ${input.staffName}. Tienes una nueva asignación BOOMBOX.\n\nEvento: ${input.eventName}\nRol: ${assignmentRoleLabel(input.role)}\nCitación Staff: ${call}\nHorario servicio: ${start}–${end}\nLugar: ${input.location??"Por confirmar"}\n\nPortal Staff: ${input.portalUrl}`,htmlBody:`<main style="font-family:Arial,sans-serif;color:#171717;line-height:1.6"><h1>Nueva asignación BOOMBOX</h1><p>Hola ${escape(input.staffName)}, revisa y responde tu asignación.</p><p><strong>Evento:</strong> ${escape(input.eventName)}<br><strong>Rol:</strong> ${escape(assignmentRoleLabel(input.role))}<br><strong>Citación Staff:</strong> ${escape(call)}<br><strong>Horario servicio:</strong> ${escape(start)}–${escape(end)}<br><strong>Lugar:</strong> ${escape(input.location??"Por confirmar")}</p><p><a href="${escape(input.portalUrl)}" style="display:inline-block;background:#F78900;color:#111;text-decoration:none;font-weight:700;padding:12px 18px;border-radius:10px">Abrir Portal Staff</a></p></main>`,driveFileIds:[]}}
