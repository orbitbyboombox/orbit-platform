"use client";

import { useState, useTransition } from "react";
import { ChevronDown, ChevronUp, GripVertical, Pencil, Plus, Trash2 } from "lucide-react";
import { Button } from "@/components/ui/button";
import { calculateOperationalGaps, sortOperationalBlocks, type OperationalBlock } from "./operational-blocks";
import { deleteOperationalBlockAction, reorderOperationalBlocksAction, saveOperationalBlockAction } from "./operational-blocks.actions";

const localValue = (value: string) => new Date(value).toLocaleString("sv-SE", { timeZone: "America/Santiago", hour12: false }).slice(0, 16).replace(" ", "T");

export function OperationalBlocksPanel({ projectId, initialBlocks }: { projectId: string; initialBlocks: readonly OperationalBlock[] }) {
  const [pending, startTransition] = useTransition();
  const [message, setMessage] = useState("");
  const [expanded, setExpanded] = useState(initialBlocks.length > 0);
  const [editing, setEditing] = useState<string | null>(null);
  const blocks = sortOperationalBlocks(initialBlocks);
  const gaps = calculateOperationalGaps(blocks);
  const run = (work: () => Promise<{ ok: boolean; message?: string; error?: string }>) => startTransition(async () => { const result = await work(); setMessage(result.ok ? result.message ?? "Actualizado." : result.error ?? "No fue posible actualizar."); });
  const move = (index: number, direction: -1 | 1) => { const target = index + direction; if (target < 0 || target >= blocks.length) return; const ordered = blocks.map((block) => block.id); [ordered[index], ordered[target]] = [ordered[target], ordered[index]]; run(() => reorderOperationalBlocksAction(projectId, ordered)); };
  return <section className="rounded-2xl border bg-card p-5 sm:p-6" id="operational-blocks" aria-busy={pending}>
    <div className="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-brand">Planificación operacional</p><h2 className="mt-1 text-xl font-semibold">PLANIFICACIÓN POR BLOQUES</h2><p className="mt-1 text-sm text-muted">Divide la operación interna sin crear otra cotización, reserva ni evento de Calendar.</p></div><Button variant="outline" type="button" onClick={() => setExpanded((value) => !value)} aria-expanded={expanded}>{expanded ? "Ocultar" : blocks.length ? "Ver bloques" : "Dividir evento"}<ChevronDown className="size-4" /></Button></div>
    {expanded ? <>
      <div className="mt-5 space-y-3">{blocks.map((block, index) => editing === block.id ? <form key={block.id} className="grid gap-3 rounded-xl border border-brand/30 bg-brand/5 p-4 sm:grid-cols-2" action={(data) => { run(() => saveOperationalBlockAction(data)); setEditing(null); }}><input type="hidden" name="projectId" value={projectId}/><input type="hidden" name="id" value={block.id}/><label className="text-sm font-medium">Nombre<input required name="name" defaultValue={block.name} className="mt-1 min-h-11 w-full rounded-xl border bg-background px-3" /></label><label className="text-sm font-medium">Inicio<input required type="datetime-local" name="startAt" defaultValue={localValue(block.startAt)} step={60} className="mt-1 min-h-11 w-full rounded-xl border bg-background px-3" /></label><label className="text-sm font-medium">Fin<input required type="datetime-local" name="endAt" defaultValue={localValue(block.endAt)} step={60} className="mt-1 min-h-11 w-full rounded-xl border bg-background px-3" /></label><label className="text-sm font-medium">Notas<input name="notes" defaultValue={block.notes ?? ""} className="mt-1 min-h-11 w-full rounded-xl border bg-background px-3" /></label><div className="flex gap-2 sm:col-span-2"><Button disabled={pending} type="submit">Guardar</Button><Button disabled={pending} type="button" variant="ghost" onClick={() => setEditing(null)}>Cancelar</Button></div></form> : <div className="flex flex-col gap-3 rounded-xl border bg-background/30 p-3 sm:flex-row sm:items-center" key={block.id}><GripVertical className="hidden size-4 text-muted sm:block" aria-hidden="true"/><div className="min-w-0 flex-1"><p className="font-semibold">{block.name}</p><p className="text-sm text-muted">{new Date(block.startAt).toLocaleString("es-CL", { timeZone: "America/Santiago", dateStyle: "short", timeStyle: "short" })} → {new Date(block.endAt).toLocaleTimeString("es-CL", { timeZone: "America/Santiago", hour: "2-digit", minute: "2-digit" })}</p>{block.notes ? <p className="mt-1 text-xs text-muted">{block.notes}</p> : null}</div><span className="text-xs font-semibold uppercase text-brand">{block.status}</span><div className="flex items-center gap-1"><Button type="button" variant="ghost" disabled={pending || index === 0} onClick={() => move(index, -1)} aria-label={`Subir ${block.name}`}><ChevronUp className="size-4"/></Button><Button type="button" variant="ghost" disabled={pending || index === blocks.length - 1} onClick={() => move(index, 1)} aria-label={`Bajar ${block.name}`}><ChevronDown className="size-4"/></Button><Button type="button" variant="ghost" disabled={pending} onClick={() => setEditing(block.id)} aria-label={`Editar ${block.name}`}><Pencil className="size-4"/></Button><Button type="button" variant="ghost" disabled={pending} onClick={() => run(() => deleteOperationalBlockAction(projectId, block.id))} aria-label={`Eliminar ${block.name}`}><Trash2 className="size-4"/></Button></div></div>)}{gaps.map((gap) => <div className="rounded-xl border border-dashed p-3 text-sm text-muted" key={`${gap.startAt}-${gap.endAt}`}>Pausa · {gap.durationMinutes} min</div>)}</div>
      <form className="mt-5 grid gap-3 rounded-xl border border-brand/20 bg-brand/5 p-4 sm:grid-cols-2" action={(data) => run(() => saveOperationalBlockAction(data))}><input type="hidden" name="projectId" value={projectId}/><input type="hidden" name="sequence" value={blocks.length + 1}/><label className="text-sm font-medium">Nombre<input required name="name" className="mt-1 min-h-11 w-full rounded-xl border bg-background px-3" placeholder="Ej. Bloque AM"/></label><label className="text-sm font-medium">Inicio<input required type="datetime-local" name="startAt" step={60} className="mt-1 min-h-11 w-full rounded-xl border bg-background px-3"/></label><label className="text-sm font-medium">Fin<input required type="datetime-local" name="endAt" step={60} className="mt-1 min-h-11 w-full rounded-xl border bg-background px-3"/></label><label className="text-sm font-medium">Notas<input name="notes" className="mt-1 min-h-11 w-full rounded-xl border bg-background px-3" placeholder="Montaje, pausa o indicaciones"/></label><Button className="sm:col-span-2" disabled={pending} type="submit"><Plus className="size-4"/>Agregar bloque</Button></form>
    </> : null}
    {message ? <p aria-live="polite" className="mt-3 text-sm text-muted">{message}</p> : null}
  </section>;
}
