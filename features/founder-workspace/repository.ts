import"server-only";
import type{SupabaseClient}from"@supabase/supabase-js";
import{DEFAULT_WORKSPACE,type FounderWorkspacePreferences,type QuickActionKey,type WorkspaceWidgetKey}from"./catalog";
export async function loadFounderWorkspace(client:SupabaseClient,userId:string):Promise<FounderWorkspacePreferences>{const{data,error}=await client.from("founder_workspace_preferences").select("quick_action_order,hidden_quick_actions,favorite_quick_actions,widget_order,hidden_widgets").eq("user_id",userId).maybeSingle();if(error){if(error.code==="42P01")return structuredClone(DEFAULT_WORKSPACE);throw error}if(!data)return structuredClone(DEFAULT_WORKSPACE);return{quickActionOrder:data.quick_action_order as QuickActionKey[],hiddenQuickActions:data.hidden_quick_actions as QuickActionKey[],favoriteQuickActions:data.favorite_quick_actions as QuickActionKey[],widgetOrder:data.widget_order as WorkspaceWidgetKey[],hiddenWidgets:data.hidden_widgets as WorkspaceWidgetKey[]}}
