"use server";import{revalidatePath}from"next/cache";import{createSupabaseServerActionClient}from"@/lib/supabase/server";import type{OperationalMilestone}from"./types";
type Result={ok:true;message:string}|{ok:false;error:string};const failed=(e:unknown):Result=>({ok:false,error:e instanceof Error?e.message:"No fue posible actualizar el checklist."});
export async function setChecklistItemAction(projectId:string,itemId:string,completed:boolean):Promise<Result>{try{const client=await createSupabaseServerActionClient();const{error}=await client.rpc("set_event_checklist_item",{p_item_id:itemId,p_completed:completed});if(error)throw error;refresh(projectId);return{ok:true,message:completed?"Control confirmado.":"Control marcado como pendiente."}}catch(e){return failed(e)}}
export async function recordOperationalMilestoneAction(projectId:string,milestone:OperationalMilestone,notes?:string):Promise<Result>{try{const client=await createSupabaseServerActionClient();const{error}=await client.rpc("record_event_milestone",{p_project_id:projectId,p_milestone:milestone,p_notes:notes??null});if(error){if(error.code==="23505")throw new Error("Este hito ya fue registrado.");throw error}refresh(projectId);return{ok:true,message:"Hito operacional registrado."}}catch(e){return failed(e)}}
function refresh(projectId:string){revalidatePath(`/projects/${projectId}`);revalidatePath("/operations");revalidatePath("/tasks");revalidatePath("/notifications")}
