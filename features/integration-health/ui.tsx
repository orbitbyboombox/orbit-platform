"use client";
import { useTransition } from "react";
import { useRouter } from "next/navigation";
import { Activity, RefreshCw, ShieldCheck } from "lucide-react";
import type { IntegrationHealthSnapshot, IntegrationSignal } from "./types";
const colors: Record<IntegrationSignal["status"], string> = { PASS: "text-success bg-success/10 border-success/25", PENDIENTE: "text-warning bg-warning/10 border-warning/25", ERROR: "text-danger bg-danger/10 border-danger/25", OFF: "text-muted bg-background border-border", "NO CONFIGURADO": "text-muted bg-background border-border" };
function Card({ title, signals }: { title: string; signals: readonly IntegrationSignal[] }) { return <section className="rounded-[1.5rem] border bg-card p-5 sm:p-7"><h2 className="text-lg font-semibold">{title}</h2><div className="mt-4 divide-y">{signals.map((item) => <div className="flex items-start justify-between gap-4 py-3 first:pt-0 last:pb-0" key={item.label}><div className="min-w-0"><p className="text-sm font-medium">{item.label}</p>{item.detail && <p className="mt-1 text-xs leading-5 text-muted">{item.detail}</p>}</div><span className={`shrink-0 rounded-full border px-2.5 py-1 text-[10px] font-semibold uppercase tracking-wide ${colors[item.status]}`}>{item.status}</span></div>)}</div></section> }
export function IntegrationHealthCenter({ snapshot }: { snapshot: IntegrationHealthSnapshot }) { const router = useRouter(); const [pending, start] = useTransition(); return <div className="space-y-6 pb-12"><header className="rounded-[2rem] border bg-card p-6 sm:p-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[.18em] text-brand"><Activity className="size-4" /> Estado de Integraciones</div><h1 className="mt-3 text-3xl font-semibold tracking-tight">ORBIT está saludable.</h1><p className="mt-2 text-sm text-muted">Panel Founder de observabilidad. Solo lectura; no cambia configuraciones ni envía comunicaciones.</p></div><button className="inline-flex min-h-10 items-center justify-center gap-2 rounded-xl border px-3 text-sm font-medium hover:bg-background disabled:opacity-60" disabled={pending} onClick={() => start(() => router.refresh())}><RefreshCw className={`size-4 ${pending ? "animate-spin" : ""}`} />Actualizar estado</button></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{snapshot.summary.map((item) => <div className="rounded-2xl border bg-background/40 p-4" key={item.label}><p className="text-xs text-muted">{item.label}</p><p className={`mt-2 text-sm font-semibold ${colors[item.status].split(" ")[0]}`}>{item.status}</p><p className="mt-1 text-xs text-muted">{item.detail}</p></div>)}</div><p className="mt-5 flex items-center gap-2 text-xs text-muted"><ShieldCheck className="size-4" />Actualizado: {new Date(snapshot.checkedAt).toLocaleString("es-CL")} · Acceso {snapshot.accessRole}</p></header><div className="grid gap-6 xl:grid-cols-2"><Card title="WhatsApp Business" signals={snapshot.whatsapp} /><Card title="BOOMBOX Web" signals={snapshot.boombox} /><Card title="Google" signals={snapshot.google} /><Card title="ORBIT Production" signals={snapshot.orbit} /></div></div> }
