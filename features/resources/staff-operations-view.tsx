import Link from "next/link";

export type StaffOperationsEvent={id:string;date:string;customer:string;service:string;published:boolean;assignments:string[]};
export type StaffOperationsRequest={id:string;projectId:string;staff:string;responsibility:string;event:string;date:string};

export function StaffOperationsView({events,requests}:{events:StaffOperationsEvent[];requests:StaffOperationsRequest[]}){
  return <section className="space-y-5 rounded-2xl border bg-card p-5 sm:p-7"><header><p className="text-xs font-semibold uppercase tracking-[.18em] text-brand">Operaciones · próximos 15 días</p><h2 className="mt-2 text-2xl font-semibold">Planificación operacional</h2><p className="mt-2 text-sm text-muted">Vista del Evento. Las asignaciones, solicitudes y pagos se administran exclusivamente al abrir el Evento.</p></header><div className="grid gap-5 xl:grid-cols-[1.3fr_.7fr]"><section><h3 className="font-semibold">Eventos publicados y asignaciones</h3><div className="mt-3 space-y-3">{events.map(event=><article className="rounded-xl border p-4" key={event.id}><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-semibold">{event.date} · {event.customer}</p><p className="mt-1 text-sm text-muted">{event.service}</p></div><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${event.published?"bg-emerald-500/10 text-emerald-500":"bg-amber-500/10 text-amber-500"}`}>{event.published?"Publicado":"No publicado"}</span></div><p className="mt-3 text-sm">{event.assignments.length?event.assignments.join(" · "):"Sin asignaciones confirmadas"}</p><Link className="mt-3 inline-block text-sm font-semibold text-brand" href={`/projects/${event.id}`}>Abrir Evento para administrar</Link></article>)}{!events.length?<p className="rounded-xl border border-dashed p-6 text-sm text-muted">No hay eventos durante los próximos 15 días.</p>:null}</div></section><section><h3 className="font-semibold">Solicitudes pendientes</h3><div className="mt-3 space-y-3">{requests.map(request=><article className="rounded-xl border p-4" key={request.id}><p className="font-semibold">{request.staff} · {request.responsibility}</p><p className="mt-1 text-sm text-muted">{request.event} · {request.date}</p><Link className="mt-3 inline-block text-sm font-semibold text-brand" href={`/projects/${request.projectId}#staff-assignment`}>Revisar en el Evento</Link></article>)}{!requests.length?<p className="rounded-xl border border-dashed p-6 text-sm text-muted">No hay solicitudes pendientes.</p>:null}</div></section></div></section>;
}
