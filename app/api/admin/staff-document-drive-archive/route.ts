import{timingSafeEqual}from"node:crypto";import{NextResponse}from"next/server";import{createAdminClient}from"@/lib/supabase/admin";import{syncStaffArchiveBackfill}from"@/features/staff-monthly-account/drive-archive.service";
export async function POST(request:Request){const expected=process.env.SUPABASE_SERVICE_ROLE_KEY??"",provided=request.headers.get("authorization")?.replace(/^Bearer\s+/i,"")??"",authorized=expected.length===provided.length&&expected.length>0&&timingSafeEqual(Buffer.from(expected),Buffer.from(provided));if(!authorized)return NextResponse.json({message:"No autorizado."},{status:403});const results=await syncStaffArchiveBackfill(createAdminClient());return NextResponse.json({processed:results.length,synced:results.filter(x=>x.status!=="ERROR").length,failed:results.filter(x=>x.status==="ERROR").length,results})}
