import { redirect } from "next/navigation";
import { createSupabaseServerClient } from "@/lib/supabase/server";
import { StaffExpenseReview } from "@/features/staff-expenses/staff-expense-review";
export default async function StaffExpensesPage({params,searchParams}:{params:Promise<{projectId:string}>;searchParams:Promise<{reviewExpense?:string}>}){const[{projectId},{reviewExpense}]=await Promise.all([params,searchParams]),client=await createSupabaseServerClient();const{data:user}=await client.auth.getUser();if(!user.user)redirect("/login");const[{data:project},{data:items},{data:assignments},{data:settlements}]=await Promise.all([client.from("projects").select("id,name,orbit_event_id").eq("id",projectId).single(),client.from("staff_expense_submissions").select("id,staff_id,project_id,category,amount,occurred_on,payment_method,description,notes,receipt_path,status,reimbursement,submitted_at,rejection_reason,materialized_expense_id,staff(first_name,last_name)").eq("project_id",projectId).order("submitted_at",{ascending:false}),client.from("assignments").select("id,staff_id,status").eq("project_id",projectId).is("deleted_at",null).in("status",["CONFIRMED","ACCEPTED","COMPLETED"]),client.from("event_staff_payments").select("id,staff_id,status").eq("project_id",projectId).eq("status","CONFIRMED").is("deleted_at",null)]);if(!project)redirect("/projects");const enriched=(items??[]).map(item=>{const matchingAssignments=(assignments??[]).filter(row=>row.staff_id===item.staff_id);const settlement=(settlements??[]).find(row=>row.staff_id===item.staff_id);return{...item,assignment_count:matchingAssignments.length,assignment_status:matchingAssignments.length?"CONFIRMED":null,settlement_id:settlement?.id??null,settlement_status:settlement?.status??null};});return <StaffExpenseReview focusId={reviewExpense} items={enriched} project={project}/>;}
